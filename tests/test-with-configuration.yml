name: Test With Configuration Headers
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        with:
          endpoint: 'https://api.github.com/repos/actions/checkout'
          configuration: '{"method": "GET", "headers": {"Accept": "application/vnd.github.v3+json", "User-Agent": "fetch-api-data-action-test"}}'
          set-output: 'true'
          debug: 'true'

      - name: Assert output is non-empty
        env:
          ACTUAL: ${{ steps.action.outputs.fetchApiData }}
        run: |
          echo "actual: $ACTUAL"
          if [ -z "$ACTUAL" ]; then
            echo "FAIL: expected fetchApiData output to be non-empty"
            exit 1
          fi
          echo "PASS: fetchApiData output is non-empty"

      - name: Assert output contains expected repo fields
        env:
          ACTUAL: ${{ steps.action.outputs.fetchApiData }}
        run: |
          echo "actual: $ACTUAL"
          if ! echo "$ACTUAL" | grep -q '"id"'; then
            echo "FAIL: expected output to contain 'id' field"
            exit 1
          fi
          if ! echo "$ACTUAL" | grep -q '"name"'; then
            echo "FAIL: expected output to contain 'name' field"
            exit 1
          fi
          echo "PASS: output contains expected repo fields"

      - name: Assert file is saved
        run: |
          FILE1="$GITHUB_WORKSPACE/fetch-api-data-action/data.json"
          FILE2="fetch-api-data-action/data.json"
          if [ -f "$FILE1" ]; then
            echo "PASS: file '$FILE1' exists"
          elif [ -f "$FILE2" ]; then
            echo "PASS: file '$FILE2' exists"
          else
            echo "FAIL: expected file 'fetch-api-data-action/data.json' to exist"
            echo "GITHUB_WORKSPACE=$GITHUB_WORKSPACE"
            echo "PWD=$(pwd)"
            ls -la || true
            ls -la "$GITHUB_WORKSPACE" || true
            exit 1
          fi

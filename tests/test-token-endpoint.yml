name: Test token-endpoint and token-configuration inputs
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        with:
          endpoint: 'https://jsonplaceholder.typicode.com/todos/1'
          token-endpoint: 'https://jsonplaceholder.typicode.com/posts/1'
          token-configuration: '{"method": "GET"}'
          set-output: 'true'
          save-location: 'fetch-api-data-action'
          save-name: 'token-endpoint-test'

      - name: Assert output is non-empty
        env:
          ACTUAL: ${{ steps.action.outputs.fetchApiData }}
        run: |
          echo "actual: $ACTUAL"
          if [ -z "$ACTUAL" ]; then
            echo "FAIL: expected non-empty fetchApiData output"
            exit 1
          fi
          echo "PASS: got output when using token-endpoint and token-configuration"

      - name: Assert saved file exists
        run: |
          FILE="fetch-api-data-action/token-endpoint-test.json"
          if [ ! -f "$FILE" ]; then
            echo "FAIL: expected file $FILE not found"
            exit 1
          fi
          echo "PASS: file $FILE exists"
          cat "$FILE"
